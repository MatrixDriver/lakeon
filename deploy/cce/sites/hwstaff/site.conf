# hwstaff_l563903 站点配置
SITE_DESCRIPTION="华为云培训账号 hwstaff_l563903 (北京四)"

# Kubernetes
# Legacy single-cluster scripts still point at the current production cluster.
SITE_KUBECONFIG="$HOME/.kube/cce-lakeon-config"
SITE_CONTROL_KUBECONFIG="$HOME/.kube/cce-dbay-control-plane-config"
SITE_DATA_KUBECONFIG="$HOME/.kube/cce-lakeon-config"

# SWR 镜像仓库
SWR_ORG="flex"

# 凭据变量名 (在 .env 中定义)
# HWCLOUD_AK, HWCLOUD_SK, RDS_PRIVATE_IP, RDS_PASSWORD

# ELB
ELB_EIP="114.116.207.156"
CCE_EIP="114.116.213.150"
